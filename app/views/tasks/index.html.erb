<h1>やること一覧</h1>
<br>

<%= link_to '新規作成', new_task_path, class: "btn btn-primary" %>
<br>
<table class="table table-striped table-hover">
  <thead>
    <tr>
      <th>User名</th>
      <th>To do</th>
      <th>メモ</th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @tasks.each do |task| %>
    <tr>
      <!--userと紐づけることによって表示する内容-->
      <td><%= task.user.name %></td>
      <td><%= task.title %></td>
      <td><%= task.body %></td>
      <!--current_user Action ログインしているuserのみ可能なアクション-->
      <% if task.user == current_user %>
      <td><%= link_to '削除', task, method: :delete, data: { confirm: '本当に削除しますか?' }, class: "btn btn-danger" %></td>
      <% end %>
    </tr>
    <% end %>
  </tbody>
</table>


