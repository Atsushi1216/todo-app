<header>
    <nav class="navbar navbar-expand-lg navbar-light">
        <div class="container">
            <div class="col-7　ml-auto">
                <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse ml-auto" id="navbarNavDropdown">
                    <ul class="navbar-nav  ml-auto">
                        <% if user_signed_in? %>
                          <li><%= link_to ' To do', new_task_path, class: ' nav-link text-body' %></li>
                          <li><%= link_to ' Log Out', destroy_user_session_path, method: :delete,class: 'nav-link text-body' %></li>
                        <% else %>
                          <li><%= link_to ' Sigh Up', new_user_registration_path,class: 'nav-link text-body' %></li>
                          <li><%= link_to ' Log In', new_user_session_path ,class: 'nav-link text-body'%></li>
                        <% end %>
                    </ul>
                </div>
            </div>
        </div>
    </nav>